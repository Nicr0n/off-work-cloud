server:
  port: 10000
spring:
  application:
    name: test-provider #应用名
  cloud:
    nacos:
      discovery:  #发现中心
        server-addr: your.ip.address  #nacos地址
        group: OFF_WORK_CLOUD  #分组名
      config:  #配置中心
        server-addr: your.ip.address  #nacos地址
        file-extension: yml  #配置文件类型
        group: OFF_WORK_CLOUD  #分组名